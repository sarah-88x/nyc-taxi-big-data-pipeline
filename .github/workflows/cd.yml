name: Data Pipeline CD

on:
  push:
    branches: [ main ]

jobs:
  validate-deployment:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Validate Docker Setup
        run: |
          docker compose -f docker/docker-compose.yml config
          echo "CD blueprint validated successfully."